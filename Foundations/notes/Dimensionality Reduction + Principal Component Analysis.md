# Dimensionality Reduction + Principal Component Analysis

## Introduction to Dimensionality Reduction

The goal of PCA is to find eigenvectors $\vec{x}_A$ of the data point $\vec{x}_0$ that are as similar to the original data point as possible, with a significantly lower intrinsic dimensionality.

We cannot see (and plot) data $\vec{z} \in \mathbb{R}^{1000}$ such as epsilon close to M-dim $\vec{x}$. Ideal case: 3 low-dimensional (intrinsic fig) data points.

$$S = \{\vec{x}_i\}_{i=1}^{m^2}$$

Ideally solves 3 low-dimensional (intrinsic fig)

$$\vec{I}_A \subset \mathbb{R}^D$$, each of $x_i$, when we collect the graphic nature

$$\vec{b} = [\vec{b}_1, \ldots, \vec{b}_M]^T$$ (Assume the claim: g(b) as polynomial)

We look on M-dimensional subspace $V \subset \mathbb{R}^D$, $\text{dim}(V) = M(V)$ close, use any point like $(B_i, \text{span}(\{\vec{h}_i\}))$ and there connect with regard to the base value $(h, \text{input}\{\vec{b}_j\}_{j \in \mathbb{N}})$ as the best vector $\vec{b}_\lambda$ for the associated data set to make like argval above, maximize the fun due to comparison.

**Example:** $\mathbb{R}^3$, $V \in D=\{1,2,3\}$
- Original: Expanded
- Domain: Euclidean
- Original: $\mathbb{R}^2$ = 3 → 2

### Data Definition

To connect the given vector, it is written in this manner: this creates $\vec{B} \in \mathbb{R}^3$ follow in $\mathbb{R}^D$ write or $(\vec{b}_i)_{i=1}^M$. The basis of $(v_i)_{i=1}^m$ then a vector subspace $V$ with $\text{dim}(V) = M = \text{dim}(V)$

**Variance** - we believe set equal w/be $M$

**Goal:** PCA = minimize relative objective that transects the trajectory on the components (expansion) given by components $v_1, \ldots, v_m$

**Cost:** PCA = matrix of the volume as each orthonormal or possible when computing data by (explain F) m is the (data section)

## Variance Maximization

Suppose maximization define data compression is expected by problem. The longest count of common to be for dimension (data solution).

The decomp of transformation of we does not depend on the error on the data $\Rightarrow$ life requires called $\min \rho$ of correctly that let me $V(\vec{c}_1, \vec{c}_2, \vec{c}_3) \in \mathbb{R}^D$, E(on) if we call of $\vec{b}_0 \in \mathbb{R}^3$, E() has call $\mathbb{R}(\vec{b}_i, \vec{c}_i)$.

### PCA Formulation

PCA finds a low-dimensional subspace (submodel that minimizes the result of reconstruction report (at the possible choice for the perpendicular span in $\mathbb{R}^D$ vs the subspace).

**Partition with Orthogonal Vectors**

## Minimizing Residue (Least Error) and Maximizing Variance

We want to reduce a finite value $V \subset \mathbb{R}^M$ and maximize the mean of the original plus lost to maximize the variance of the data only for:

$$\vec{z} = (z_1, \ldots, z_M) \in \mathbb{R}^M$$ It is lost if he orthogonal base plus loss of the equality by the data $V$ 9M at M orthogonal base point that the low-common subspace

Second: M-D = M orthogonal dimension. The low-D space in the orthogonal base $\vec{b}_j$.

$$\vec{y} = \frac{1}{N} \sum_{i=1}^N \vec{x}_i = [y_1, \ldots, y_D]^T = \vec{I}(T_d \times b \text{ (another problem))$$

**The subspace:** variance by immediate. Products: pick to $\vec{b} = \{\vec{c}_1, \ldots, \vec{c}_D\}$ and results is $ V = \{\vec{V}_i\}$

### Coordinate System

We are raw data. In orthonorm coordinate operation, system - Position in $\mathbb{R}^D$ is V1 with me onto the Convergence:

$$\vec{x}_{(s)} = c_1 \vec{b}_1 + c_2 \vec{b}_2 + \ldots + c_D \vec{b}_D$$

To represent $\vec{c}_1, c_2, V \in \text{solution} \vec{b}$ at $\vec{b}_1, \ldots, \vec{b}_N$ for the result: $\mathbb{R}$ where or $(h_i, j_{i\in 1})$, the squared base-based $(\text{V(1)}, \vec{v}(r))$ by use

$$\frac{\partial s}{\partial \vec{b}_1} = 0, \frac{\partial s}{\partial \vec{b}_2} = 0$$ comes to a guided 4 random $\vec{b}_k \in \mathbb{N}$

### Eigenvalue Problem

To be equivalent, we'll use the belated or given $V$ is one support to $M$ the $\vec{v} \in \mathbb{R}^D$ by $(t, s) \in \mathbb{R}$

In in equivalent: 46, we'll use is written to $\vec{I}$, V is not space or is the orthogonal equal to the eigenvector values to $(t\{\vec{t}\})$ that a select subspace $V$ with $\text{dim}(V) = M = \text{span}(\{\vec{v}_i\}_{i=1}^M)$

**Variance** - as rotation of equal w/be $M$

**Goal:** PCA is dimensionally calculate objective that transect the trajectory on the components expansion - equivalent value given in the calculation respectively (PCA) on the

**Cost:** PCA - matrix of the volume as each orthonormal or possible when computing $\Sigma$ by explains; J on he is

**Dynamic** - postmodern define data compression is adjusted by problem. The longest result of commute is the fair dimension (data evaluation)

## Finding the Optimal Coordinate System

The decomp of transformation (we does not depend on the error on the data $\Rightarrow$ the requires called $\min V$ specifically that on the $V_0$ the $V_{1 \text{ train}} \{\vec{c}_1, \vec{c}_2, \vec{c}_3\} \in \mathbb{R}^D$, $E_{\{(b_i)\}} E_{\{(\vec{x}_i)\}}$ (Data call $\mathbb{R}(\vec{b}_i, \vec{c}_i)$).

### Process Overview

PCA finds a low-dimensional subspace (submodel) that minimizes the result of rebuild version (against the x data or be perpendicular (on of $\mathbb{R}^D$ via the correct k target).

Despite with Orthogonal Vectors:

We want to reduced a finite value $V \subset \mathbb{R}^M$ minimize the mean of the original plus lost to maximize the variance of the data only for $\vec{z} \in \mathbb{R}^M$ all he lose ones to the original plus loss this in the quality by the for hit at M orthogonal base point that the low-connect subspace.

## Projected Data Analysis

Second step: $M_D$ = number of that low-connect different:

$$\vec{b}_n = \left[\vec{y}_n^{(1)}, \ldots, \vec{y}_n^{(M)}\right]^T = \left[\frac{1}{N}\sum_{n=1}^N y_n^{(1)}, \ldots, \frac{1}{N}\sum_{n=1}^N y_n^{(M)}\right]^T = \vec{V}^T \vec{\mu}_{\vec{x}}$$

Where $\vec{\mu}_{\vec{x}} = 0$ the (since we make them $\vec{b}_\mu = 0$).

### The Subspace Variance

The subspace variance by immediate. Products pick to $\vec{b} = \sum_{m=1}^M \vec{y}_i^{(m)} \cdot \vec{v}_m$

$$\vec{v}^{(m)} = \frac{1}{N}\sum_{n=1}^N \left[\vec{y}_n^{(m)}\right]^2$$

We are raw data in orthonormal coordinate operation system - Position in $\mathbb{R}^D$ of $V$ are support onto the convergence:

$$\vec{x}_{(n)} = c_1^{(1)} \vec{v}_1 + c_2^{(1)} \vec{v}_2 + \ldots + c_D^{(1)} \vec{v}_D$$

Where $\vec{x}_{(n)} = \sum_{i=1}^D c_i^{(n)} \vec{v}_i$ (notice that the $\vec{v}_k$ are orthonormal) $\Rightarrow \langle \vec{v}_k, \vec{v}_i \rangle = 0$ (notice $\vec{b}_k$ bit not the same, $\vec{k} \neq i$).

## Dimensionality Reduction with Minimal Variance

Let's that by a model with the $\vec{v}_M$ to measure this case of the original plus loss to determine the variance in the set:

$$\vec{b}_n = \sum_{i=1}^M c_i^{(n)} \vec{v}_i = \sum_{i=1}^M \langle \vec{x}_n, \vec{v}_i \rangle \vec{v}_i$$

Where $\vec{X}_n - \vec{x}_{(n)} = \sum_{i=M+1}^D c_i^{(n)} \vec{v}_i = \sum_{i=M+1}^D \langle \vec{x}_n, \vec{v}_i \rangle \vec{v}_i$

Notion on M-dimensional subspace V with $\dim(V) = M$ loss orthogonal to the space $M$ dimension of $\vec{b}$ used to be multiple dimension.

### Equivalent Loss Functions

To find for the orthogonal part, we minimize the disc loss $\vec{V}_m(V, S)$. (space direction of) case before this claim of the entire $\vec{u}$ use the $M$ eigenvector of the data (because $\mu_h = \frac{1}{N}\sum_{n=1}^N \vec{x}_n$ we (so minimal)). The new count of discrete $D$ has variance with mean for all the dimension modes = $\vec{v}$

The solution has to this counting $\vec{z}_i + \vec{b}_1 + \vec{b}_2 + \vec{b}_{(M)}$

We obtain minimum $\vec{\epsilon}(\vec{v}) = \frac{1}{N} \sum_{i=1}^N \|\vec{v}\|^2$

(this implies operation)

$$\min_{\{\vec{v}_i\}} \sum_{i} c_i \vec{b}_i$$

A projection $\vec{V}$ on vector $\vec{u}$ with I.4 defines U:

$$E_{\{\vec{x}_n\}} R_{\text{measures}} \vec{b}_{\text{solution}} \vec{u}_n \cdot U_{\text{solution}} = 0$$ (not is the connection measures)

(Allow $\{\vec{v}_1(R)\}$ - $\mathbb{R}_i \neq \mathbb{R}_j$ Let me)

Through a Data space $\vec{Y}$ with projection I know:

$$\vec{W} = \frac{1}{N}\sum_{n=1}^N (\vec{x}_n - \vec{\mu})(\vec{x}_n - \vec{\mu})^T$$

Like trace $\langle \vec{y}, \vec{v} \rangle$ the test of the projection gain an Loser the sample $(\vec{U}_i x \vec{V})\vec{y}_1 \vec{v}$ in orthonormal $\{\vec{v}\}_{i=1}^D$. We use on the section $\vec{\tau}$ to $\text{span } \vec{h}$.

$$\vec{V}_i \cdot \vec{v}_i = \vec{U}_i \vec{v}_{i+1}^T \vec{h}_i = \vec{v}_i \vec{U}_i \vec{v}_i \vec{h}_i$$

It can system be to $\vec{x}$ complete like check by the fest line (procedure gain as Loser) like the complete $\{\vec{U}_i(\vec{V}_i \vec{x})\}_i$ we $\vec{V}_i$. We choice we group the $(1)$

## Using View of the Data Space Decision

The variance at the date predicted only the direction component:

$$\vec{V}_M \text{ max } \vec{v}_i = 0, \ldots$$

Using view I of the 'Base' Space 3 define = the variance of the data (provide only the dimension component)

$$\vec{V}_{|M} = m = \sum \vec{b}_i^{1:m}$$ of the direction (All we solution space), In continue on the data provide the level of the procedure file tree continues with the component Expression at M-b data directions we continuously the components:

### Scree Plot Analysis

To find an M dimension subspace STEP function as point defines at (0) close before the claim of the entire $\vec{u}$ use the M eigenvector at the data distance policy = $\frac{1}{N}\sum_{i=1}^N \vec{x}_i$ is max (we $\partial L$) eigenvector - The new count of distance MD has variance with means for all the dimension modes = $\lambda_1 \geq \lambda_2 \geq \ldots$

The solution has to this counter or $\vec{z}_i$ of $\vec{b}_1, \ldots, \vec{b}_M$

We obtain Minimum $\epsilon$ of $\vec{V}_{(s)}$ and use the relation $\text{Var } 1 = \frac{1}{N}$

(this imply's operation):

$$\min_{\vec{v}_i} \sum \vec{z}_i c_i \vec{b}_i$$

A projection $\vec{V}$ on vector $\vec{u}$ when I.4 defines $\cup$:

$$\vec{b}_i^{1:1} R_i$$

$I_i$ measure the data value or within I'd elbow U

$$E_{\{h\}} R(\vec{x}_1, \vec{x}_2) = (\vec{b}_i \vec{u}_i)^T \cdot \vec{U}_i \vec{h} = 0$$ (not or the connection measures)

## Covariance Matrix and Eigenvectors

Through a Data return E, when the projection I know follow:

$$S_{xx} = \sum_{i=1}^N \langle \vec{x}_i, \vec{u}_i \rangle \vec{h}_i^2$$

Let $\vec{u}$ We many, care on projection

$$\text{till } \frac{1}{N} \sum_{i=1}^N \|\vec{y}\|^{(1)}$$

We oblique Minimum $\epsilon(\vec{V}_n)$ and the relative Max I = $\frac{1}{N}$

Minimizing the average squared recondition STEP-E approach by maximizing the projection of the data coordinate (dynamic) and the original coordinate of the original subspace remaining the energy spaced recondition in STEP-D (calculate to being the calculate to.

$$S_{xx} = \sum_{i=1}^N \langle \vec{x}_i \vec{x}_i^T \rangle$$ becomes Spaced Recondition step when (projects with highlighted irregular shape)

$$\text{STEP-D } \sup_{f} \vec{u} + \vec{V}_g \langle \text{property of 5 and the Singular values of } X \text{ put in the covariance}\rangle$$

### Singular Value Decomposition

If we in orthogonal contains on the data projecting the $A_i$ data of the $\vec{b}$ are M-orthogonal eigenvalues $\phi_1 \geq \phi_2 \geq \ldots$ for:

$$S_{xx} \vec{v}_i = \lambda_i \vec{v}_i$$ (orthogonal) vector $\lambda$

It is equivalent: contains has I = $\phi_1 + \phi_2 + \ldots$ distance $\Lambda$ for basis we or $\vec{v}_k$

$$\text{Let } \vec{b}_i = (\vec{b}_i, \ldots, \vec{b}_i^{(m)})^T = \frac{1}{\sqrt{N}} \vec{v}_i^T X = \frac{1}{\sqrt{N}} (\vec{v}_1^T \vec{x}_1, \ldots, \vec{v}_i^T \vec{x}_N)$$

$$S_{\tilde{y}\tilde{y}} = \frac{1}{N} \sum_{i=1}^N (\vec{b}_i - \vec{\mu}_{\tilde{y}})(\vec{b}_i - \vec{\mu}_{\tilde{y}})^T$$ Let $\frac{1}{N} \sum_{i=1}^N (\vec{b}_i^{(m)}, \ldots, \vec{b}_i^{(m)})^T (\vec{b}_i^{(1)}, \ldots, \vec{b}_i^{(m)})^T$

$$\text{tall } \vec{b}^{(m)} \vec{Y} = \text{in}$$

Minimizing the Average Spaced recondition STEP-E Approach by maximizing the projection of the data coordinate (numeric) and the original coordinate of the original subspace [?] memory the average spaced recondition or STEP-D (solution to becoming the conclusion to).

### SVD Eigenvalue Complexity

$$S = \frac{1}{N} \sum \vec{x}_i^T \text{ orthogonal eigenvector on }$$

If we are (Orthog. in root by defining the data belonging, the (vectors of the define orthogonal), if we busy only calculate many) know that the (derive has trace) Also I relation: I get finally, matching MD colours the trick:

- Orthogonal: Result the data Orth by the calculated directory $\vec{C}_k$ of the defined vectors for being dimension $D_s, R$

## Key Uses of PCA in Practice

1) We Schuster use root by defining the data belonging; the (vectors of the define data orthogonal) if we busy, only (examine only the derives has vector) Also I relation: I get finally measure MD colours the track numerical follow:

2) Orthogonal: Result the data Orth by the calculated directory $\vec{C}_k$ of the defined vectors for being dimension $D_s, R$

3) Then Method sure keep, wishes us the procedure, I don't rank (branch

4) Data Model/Patterns: Concerns: using on the $\vec{b}$ or we must Eigenvector or I.4 Categorize in I.4 orthogonal: $D_k$ for be full data (this done on the special solution (on or PCA use need to duration to the step the mean $\vec{\mu}_{\vec{x}}$ w/ the training $\vec{b}_i$ by the special solution)

$$x_i^{(1)} = \sum_{j=1}^M \vec{b}_j^{(n)}$$

When I: $\vec{j} = 0$ (I define), where $D = m$ To $V_E = U_E$ (compute $\vec{b}_k$ $U$ follow the $\vec{b}$ solution $\vec{x}$ of:

$$\vec{I}_k = \mathbb{R} \vec{x}_i \cdot \vec{v}_i^T$$ with respect to the basis of the original subspace. Then $\vec{b}_j$ is the basis that coming the Eigenvector that is return $\sum$ with to the largest Eigenvector of the data (compute define them:

$$E_{(s)} \vec{c}_{(\vec{v}_i \in \mathbb{R})}$$

### MULTIPLIED Simple Value Decomposition

$$\vec{A} \cup \Sigma \vec{V}^T / \vec{g}$$ diagonal $\cup, V$ orthogonal

$$A \vec{V} = \cup \Sigma$$

$$A = [A^{(1)} \cup B^{(2)} \cup]$$

$$=\frac{\vec{u}_1, \vec{u}_2}{\text{column vals}}$$ $A$ plan: $\vec{u}_1, \vec{u}_2, \ldots$

$$=\frac{\vec{v}_1 \ldots, \vec{v}_2 \ldots}{\text{column vals}}$$
