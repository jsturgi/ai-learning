version: '3.8'

services:
  jupyter:
    build: .
    ports:
      - "8888:8888"
    volumes:
      - ./phase1:/workspace/phase1
      - ./phase2:/workspace/phase2
      - ./phase3:/workspace/phase3
      - ./phase4:/workspace/phase4
    environment:
      - JUPYTER_ENABLE_LAB=yes